{
  "name": "festival-planner",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "smoke": "node scripts/smoke.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}

{
  "name": "festival-planner",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "smoke": "node scripts/smoke.cjs",
    "lint:i18n": "node scripts/i18n-audit.js",
    "lint:links": "node scripts/link-check.cjs",
    "test:smoke": "node scripts/smoke.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "markdown-link-check": "^3.14.2"
  }
}
